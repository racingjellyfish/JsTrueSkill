var GaussianDistribution = require('./numerics/GaussianDistribution');
var MathUtils = require('./numerics/MathUtils');

/** Container for a player's rating. **/
var Rating = function(parentGraph, teams) {
};

module.exports = Rating;
